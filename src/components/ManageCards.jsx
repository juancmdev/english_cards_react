import { useState, useEffect } from "react"

const ManageCards = () => {
const [cards, setCards] = useState([]);

    useEffect(() => {
        try {

        } catch (error) {
            console.log(error);
        }
    }, [])

  return (
    <div>
      
    </div>
  )
}

export default ManageCards
